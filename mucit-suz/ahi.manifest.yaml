# mucit-suz — AHÎ ile üretildi (kademe: kalfa)
# version manifest'te YOK (tek-evi SKILL.md frontmatter — ADR-001).
tier: kalfa
name: mucit-suz
generic_goal: "DİVAN bulgu-havuzunu acımasızca süzüp Sultan-dilinde haftada ≤3 aday öneren, her projede güvenilir-tekrarlanabilir süzme-kabuğu (T1-mekanik + T2-muhakeme)"
activation:
  onKeyword: mucit-suz
# SYS (L01) doğum-sonrası yaşam-döngüsü kanalı.
feedback:
  owner: "mucit"
  channel: skill-geri-bildirim.jsonl
  sla_tier: closure
# L24 F3: motor pakete taşındı → yol-çözümü `layiha` paketindeki hat-yolu.lib.sh'ten gelir.
# (kill-switch guard'ı YALNIZ --profil layiha'da gerekir; yoksa net hata verir — döngüsel bağ kurulmadı.)
requires:
  - layiha
install_targets:
  - _global
